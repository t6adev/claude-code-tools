plugin_id: frontend-design
registry: anthropics/claude-code   # claude plugin marketplace add <registry>
channel: claude-code-plugins        # claude plugin install <plugin_id>@<channel>
enabled: true
