plugin_id: plugin-dev
registry: anthropics/claude-plugins-official   # claude plugin marketplace add <registry>
channel: claude-code-marketplace               # claude plugin install <plugin_id>@<channel>
enabled: false  # 開発補助ツール。必要な場合は手動でインストール
