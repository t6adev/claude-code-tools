{
  "_comment": "秘密情報ファイルの読み込みをブロックするフック。.claude/settings.json の hooks セクションにマージして使う。",
  "_scripts_note": "インストーラーが command パスをインストール先に合わせて自動書き換えします。手動マージ時は実際のパスに変更してください。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "INSTALL_DIR/hooks/block-secret-files.sh"
          }
        ]
      }
    ]
  }
}
