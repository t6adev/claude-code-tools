{
  "_comment": "応答完了通知のプリセット。.claude/settings.json の hooks セクションにマージして使う。",
  "_scripts_note": "インストーラーが command パスをインストール先に合わせて自動書き換えします。手動マージ時は実際のパスに変更してください。",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "INSTALL_DIR/hooks/notify-on-stop.sh"
          }
        ]
      }
    ]
  }
}
