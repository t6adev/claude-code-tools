{
  "_comment": "安全フックのプリセット。.claude/settings.json の hooks セクションにマージして使う。",
  "_scripts_note": "インストーラーが command パスをインストール先に合わせて自動書き換えします。手動マージ時は実際のパスに変更してください。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INSTALL_DIR/hooks/block-dangerous-rm.sh"
          }
        ]
      }
    ]
  }
}
